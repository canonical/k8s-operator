name: Charm Analysis
# Unit testing, Style Linting, Type Linting, Static Analysis, and Spelling

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@645730b6cdad1c61f93566f20dfdc0d341978c1f
    secrets: inherit
    with:
      charm-directory: charms
      self-hosted-runner:  false
