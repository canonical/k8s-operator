name: Provide approval for bot PRs

on:
  pull_request:

jobs:
  bot_pr_approval:
    uses: canonical/operator-workflows/.github/workflows/bot_pr_approval.yaml@d7de61956c117646a3b42ed4568468df5bb9b73e # main
    secrets: inherit
