name: Provide approval for bot PRs

on:
  pull_request:

jobs:
  bot_pr_approval:
    uses: canonical/operator-workflows/.github/workflows/bot_pr_approval.yaml@20499a48ecff0073620125a7cb27a21ea9fc297b # main
    secrets: inherit
