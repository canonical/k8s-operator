name: Provide approval for bot PRs

on:
  pull_request:

jobs:
  bot_pr_approval:
    uses: canonical/operator-workflows/.github/workflows/bot_pr_approval.yaml@df449f1e3d1b8babbe9df48bbebcff1e58c9fda9 # main
    secrets: inherit
