name: Auto-update K8s charm libraries

on:
  schedule:
    - cron: "0 1 * * *"

jobs:
  auto-update-libs:
    uses: canonical/operator-workflows/.github/workflows/auto_update_charm_libs.yaml@645730b6cdad1c61f93566f20dfdc0d341978c1f
    secrets: inherit
    with:
      working-directory:  ./charms/worker/k8s
